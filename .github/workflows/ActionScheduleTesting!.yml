name: Scheduled and Manual Test 

on:
  schedule:
    - cron: '30 21 * * *'  # 5:30 AM MYT (Poster A)
    - cron: '0 6 * * *'    # 2:00 PM MYT (Poster A)
    - cron: '30 13 * * *'  # 9:30 PM MYT (Poster B)
  workflow_dispatch:      # This is for your manual testing

jobs:
  post-video:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pyTelegramBotAPI

      - name: Run Poster A
        # Run if it's the 5am/12pm schedule OR if you clicked "Run workflow" manually
        if: |
          github.event.schedule == '30 21 * * *' || 
          github.event.schedule == '0 6 * * *'
        # github.event_name == 'workflow_dispatch'
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python bot2.py POSTER_A

      - name: Run Poster B
        # Run if it's the 9pm schedule OR if you clicked "Run workflow" manually
        if: |
          github.event.schedule == '0 13 * * *'
        # github.event_name == 'workflow_dispatch'
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python bot2.py POSTER_B
